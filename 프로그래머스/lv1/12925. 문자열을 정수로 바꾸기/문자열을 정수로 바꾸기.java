class Solution {
    public int solution(String s) {
        int answer = 0;
        
        char[] str = s.toCharArray();
        answer = Integer.parseInt(s);
        
        if(str[0] == '+') {
            
        }
        else if(str[0] == '-'){
            
        }else {
            
        }
        
        return answer;
    }
}